# ETL-Banking-Pipeline-Project
An end-to-end ETL (Extract–Transform–Load) pipeline that pulls daily currency exchange rates from the Central Bank of Uzbekistan (CBU) public API, cleans and enriches the data with Python (pandas), loads it into SQL Server, and visualizes it in an interactive Power BI dashboard.
